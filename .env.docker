# Docker-specific URL overrides — loaded after .env in docker-compose.
# These replace localhost URLs with Docker service names.
# Do NOT put secrets here; keep them in .env.

LANGGRAPH_URL=http://langgraph:2024
MCP_SERVER_URL=http://mcp:8001
